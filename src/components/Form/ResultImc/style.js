import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 32,
  },
  text: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#FF0043',
  },
});

export default styles;
